# Tasks
- [ ] 1.1 Pure date helpers: build a month grid (weeks of days incl. leading/trailing) and group rows by their date_by day; count unscheduled
- [ ] 1.2 Calendar component: month grid, rows on their day, prev/next/today nav, date-property picker (setDateBy), open-row
- [ ] 1.3 Wire Calendar into the view-kind seam (replace the placeholder)
- [ ] 1.4 Tests: month-grid + group-by-day helpers (incl. month boundaries, invalid dates), VM setDateBy, Calendar render/navigation
- [ ] 1.5 `openspec validate collections-calendar --strict`; gates green
