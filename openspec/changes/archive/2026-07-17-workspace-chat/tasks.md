# Tasks
- [x] 1.1 `WorkspaceChatUseCases.ask` — RAG + search retrieval, LLM synthesis with history + persona, returns answer + sources; read-only
- [x] 1.2 Wire into `UseCases` + `build_container`
- [x] 1.3 `POST /workspaces/{id}/chat` endpoint
- [x] 1.4 Frontend: sidebar entry → chat panel (thread, input, source citations) + viewmodel + api client
- [x] 1.5 Tests: answer grounded in RAG + search; membership required; history respected; no-KB graceful; frontend viewmodel
- [x] 1.6 `openspec validate workspace-chat --strict`; backend + frontend gates green
