# Tasks
- [ ] 1.1 VM: edit the current view's filters/sorts (add/remove/update/reorder), persist via update_view, re-query
- [ ] 1.2 Filter builder UI: type-aware operators + value inputs; Sort builder UI: property + direction, reorderable
- [ ] 1.3 Active filter/sort count on the toolbar; empty-result state
- [ ] 1.4 Tests: VM filter/sort mutations + re-query; type-aware operator options; component render
- [ ] 1.5 `openspec validate collections-filter-sort --strict`; gates green
