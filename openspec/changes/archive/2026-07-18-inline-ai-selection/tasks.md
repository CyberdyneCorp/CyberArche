# Tasks
- [ ] 1.1 `AgentUseCases.transform_text` — per-action prompt, tool-free LLM call, view-scoped, returns transformed text only
- [ ] 1.2 `POST /documents/{id}/agent/transform` endpoint
- [ ] 1.3 Editor selection menu: Ask AI ▸ rewrite/shorten/expand/fix/translate; apply result over the selection (undoable)
- [ ] 1.4 Tests: each action calls the LLM with the text; view required; result returned unchanged; frontend flow
- [ ] 1.5 `openspec validate inline-ai-selection --strict`; gates green
