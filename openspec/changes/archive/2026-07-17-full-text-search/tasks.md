# Tasks

- [x] 1.1 `SearchUseCases.search` — title + block-content matches with snippets, access-scoped (reuse the graph's CRDT read path)
- [x] 1.2 Wire into `UseCases` + `build_container`
- [x] 1.3 `GET /workspaces/{id}/search/content` endpoint
- [x] 1.4 Frontend: command palette shows content snippets + an "Ask AI" (RAG) row
- [x] 1.5 Tests: title match, content match + snippet, access-scoping, empty query; frontend palette
- [x] 1.6 `openspec validate full-text-search --strict`; backend + frontend gates green
