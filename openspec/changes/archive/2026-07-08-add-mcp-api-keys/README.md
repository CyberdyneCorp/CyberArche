# add-mcp-api-keys

Personal API keys for connecting external MCP clients (Claude, ChatGPT) to the CyberArche MCP server: create/revoke in the UI, hashed at rest, verified alongside JWTs on every surface.
